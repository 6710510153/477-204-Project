<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hungry Shop | Fuel Your Ambition</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #ABE7B2;
            --dark-color: #1A1A1A;
            --light-bg: #F9F9F9;
            --white: #FFFFFF;
            --text-gray: #555555;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            color: var(--dark-color);
            background-color: var(--white);
        }

        /* --- Navigation --- */
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 10%;
            background: var(--white);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 700;
            letter-spacing: -1px;
        }

        .nav-links a {
            text-decoration: none;
            color: var(--dark-color);
            margin-left: 30px;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .btn-outline {
            border: 1px solid var(--dark-color);
            padding: 8px 20px;
            border-radius: 5px;
            transition: 0.3s;
        }

        .btn-solid {
            background-color: var(--primary-color);
            padding: 8px 20px;
            border-radius: 5px;
            font-weight: 600 !important;
        }

        /* --- Hero Section --- */
        .hero {
            padding: 100px 10%;
            background-color: var(--light-bg);
            text-align: center;
        }

        .hero h1 {
            font-size: 3.5rem;
            line-height: 1.1;
            margin-bottom: 20px;
            font-weight: 700;
        }

        .hero p {
            font-size: 1.2rem;
            color: var(--text-gray);
            margin-bottom: 40px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .cta-main {
            background-color: var(--primary-color);
            color: var(--dark-color);
            padding: 18px 40px;
            text-decoration: none;
            font-weight: 700;
            border-radius: 8px;
            font-size: 1.1rem;
            display: inline-block;
            transition: transform 0.2s;
        }

        .cta-main:hover {
            transform: translateY(-3px);
        }

        /* --- Features Section --- */
        .features {
            padding: 80px 10%;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
        }

        .feature-card {
            padding: 30px;
            border-radius: 15px;
            background: var(--white);
            border: 1px solid #eee;
        }

        .feature-card h3 {
            margin-bottom: 15px;
            color: var(--dark-color);
        }

        .feature-card span {
            color: var(--primary-color);
            font-size: 2rem;
            display: block;
            margin-bottom: 10px;
        }

        /* --- Comparison Table --- */
        .comparison {
            padding: 80px 10%;
            background: #fff;
        }

        .comparison h2 {
            text-align: center;
            margin-bottom: 50px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            max-width: 800px;
            margin: 0 auto;
        }

        th, td {
            padding: 20px;
            text-align: left;
            border-bottom: 1px solid #eee;
        }

        th {
            font-size: 1.2rem;
        }

        .highlight {
            background-color: rgba(171, 231, 178, 0.2);
            font-weight: 600;
        }

        /* --- Final CTA --- */
        .final-cta {
            background-color: var(--primary-color);
            padding: 100px 10%;
            text-align: center;
        }

        .final-cta h2 {
            font-size: 2.5rem;
            margin-bottom: 30px;
        }

        .btn-dark {
            background-color: var(--dark-color);
            color: var(--white);
            padding: 18px 40px;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
        }

        /* --- Footer --- */
        footer {
            padding: 40px 10%;
            text-align: center;
            font-size: 0.8rem;
            color: #888;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero h1 { font-size: 2.5rem; }
            .nav-links { display: none; }
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">Hungry Shop.</div>
        <div class="nav-links">
            <a href="#">How it Works</a>
            <a href="#">Pricing</a>
            <a href="#" class="btn-outline">Log In</a>
            <a href="#" class="btn-solid">Get Started</a>
        </div>
    </nav>

    <header class="hero">
        <h1>Fuel your ambition,<br>without the friction.</h1>
        <p>The smarter way to nourish your lifestyle. We handle the sourcing; you handle the greatness.</p>
        <a href="#" class="cta-main">Build Your Box</a>
        <p style="margin-top: 15px; font-size: 0.8rem;">No commitment. Pause or cancel anytime.</p>
    </header>

    <section class="comparison">
        <h2>Why Hungry Shop?</h2>
        <table>
            <tr>
                <th>The Old Way</th>
                <th class="highlight">The Hungry Shop Way</th>
            </tr>
            <tr>
                <td>Decision fatigue and long aisles.</td>
                <td class="highlight">Curated selections for you.</td>
            </tr>
            <tr>
                <td>Hidden additives.</td>
                <td class="highlight">100% Transparency.</td>
            </tr>
            <tr>
                <td>Heavy bags and wasted time.</td>
                <td class="highlight">Delivered to your door.</td>
            </tr>
        </table>
    </section>

    <section class="features">
        <div class="feature-card">
            <span>⚡️</span>
            <h3>Precision Sourcing</h3>
            <p>We vet every vendor to ensure your fuel is high-performance.</p>
        </div>
        <div class="feature-card">
            <span>🌿</span>
            <h3>Sustainably Minty</h3>
            <p>Our packaging is fully compostable and earth-positive.</p>
        </div>
        <div class="feature-card">
            <span>🎯</span>
            <h3>Smart Subscriptions</h3>
            <p>Our AI learns your rhythm so you never hit "Empty".</p>
        </div>
    </section>

    <section class="final-cta">
        <h2>Ready to feed your focus?</h2>
        <a href="#" class="btn-dark">Join Hungry Shop Today</a>
    </section>

    <footer>
        Hungry Shop. © 2026 | Privacy Policy | Terms of Service
    </footer>

</body>
</html>